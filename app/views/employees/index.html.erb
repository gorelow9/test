<%= link_to "New employee", new_employee_path %>
<table>
		<tr>
			<th>
				FIO
			</th>
			<th>
			</th>
			<th>
			</th>
		</tr>
		<% @employees.each do |employee| %>
		<tr>
			<td>
				<%= employee.FIO %>
			</td>
			<td>
				<%= link_to "Edit", edit_employee_path(employee) %>
			</td>
			<td>
				<%= link_to "Connect to department", new_bound_path(employee: employee.id) %>
			</td>
		</tr>
		<%end%>
	</table>