<%= form_for @department do |f| %>
  	<%= f.label 'Name' %><br>
	  <%= f.text_field :current_name %><br>
	  <%= f.label 'Date of name changing' %><br>
	  <%= f.date_field :date, as: :date, value: f.object.try(:strftime,"%m/%d/%Y") %><br>
  <%= f.submit "Submit department" %>
<% end %>